# 前后端对接指南

## 当前状况
你的前端代码已经基本完成，但缺少与后端的实际数据交互。目前下载按钮只是显示提示信息，没有真正的下载功能。

## 需要对接的关键点

### 1. 后端接口 (你的搭档需要实现)

你的搭档需要在 `main_app/views.py` 中添加以下接口：

#### A. 数据下载接口 (`/api/download-data/`)
```python
@login_required
@require_http_methods(["POST"])
def download_data(request):
    # 接收参数：
    # - category: 数据类别 (如 'CN environmental data')
    # - pollutant: 污染物类型 (如 'PM2.5')
    # - time_range: 时间范围类型 ('year' 或 'month')
    # - start_date: 开始日期
    # - end_date: 结束日期
    
    # 返回CSV文件下载
    pass
```

#### B. 数据导出接口 (`/api/export-data/`)
```python
@login_required
def export_data(request):
    # 返回JSON格式的数据文件
    pass
```

#### C. 数据预览接口 (`/api/preview-data/`)
```python
@login_required
def preview_data(request):
    # 返回JSON格式的预览数据
    pass
```

#### D. 数据统计接口 (`/api/data-statistics/`)
```python
@login_required
def data_statistics(request):
    # 返回数据统计信息
    pass
```

### 2. 前端修改 (已完成)

前端代码已经修改为能够：
- 收集用户选择的参数（数据类别、污染物类型、时间范围等）
- 发送POST请求到后端接口
- 处理文件下载响应
- 显示错误信息

### 3. URL路由 (已添加)

已在 `main_app/urls.py` 中添加了相应的路由：
```python
# 数据下载相关接口
path('api/download-data/', views.download_data, name='download_data'),
path('api/export-data/', views.export_data, name='export_data'),
path('api/preview-data/', views.preview_data, name='preview_data'),
path('api/data-statistics/', views.data_statistics, name='data_statistics'),
```

## 具体对接步骤

### 第1步：后端实现基础接口
你的搭档需要参考 `backend_views_template.py` 文件，实现上述4个接口的基本框架。

### 第2步：数据存储和查询
确定数据的存储方式：
- 数据库存储：需要创建相应的模型
- 文件存储：需要确定文件格式和位置
- 外部API：需要确定API接口

### 第3步：参数处理
后端接口需要正确处理前端传来的参数：
```python
category = request.POST.get('category')      # 'CN environmental data' 或 'UK environmental data'
pollutant = request.POST.get('pollutant')    # 如 'PM2.5', '温度', 'NDVI' 等
time_range = request.POST.get('time_range')  # 'year' 或 'month'
start_date = request.POST.get('start_date')  # 如 '2023' 或 '2023-01'
end_date = request.POST.get('end_date')      # 如 '2023' 或 '2023-12'
```

### 第4步：文件生成和下载
后端需要根据参数生成相应格式的数据文件：
- CSV格式：用于数据分析
- JSON格式：用于程序处理

### 第5步：错误处理
实现完善的错误处理机制：
- 参数验证
- 数据查询异常
- 文件生成错误
- 权限检查

## 测试建议

### 1. 接口测试
使用Postman或类似工具测试后端接口：
```
POST /api/download-data/
Content-Type: multipart/form-data

category=CN environmental data
pollutant=PM2.5
time_range=month
start_date=2023-01
end_date=2023-12
```

### 2. 前端集成测试
在浏览器中测试完整流程：
1. 登录系统
2. 选择数据类别
3. 选择污染物类型
4. 设置时间范围
5. 同意协议
6. 点击下载按钮
7. 检查文件是否正确下载

### 3. 错误场景测试
- 未登录状态下载
- 未同意协议下载
- 无效参数下载
- 网络错误处理

## 数据格式示例

### CSV格式示例（中国大气污染数据）：
```csv
日期,城市,PM2.5,PM10,NO2,O3,AQI
2023-01,北京,45,78,32,156,89
2023-01,上海,38,65,28,145,76
```

### JSON格式示例：
```json
{
  "metadata": {
    "category": "CN environmental data",
    "pollutant": "PM2.5",
    "time_range": "month",
    "start_date": "2023-01",
    "end_date": "2023-12",
    "export_time": "2025-01-15T10:30:00",
    "total_records": 84
  },
  "data": [
    ["2023-01", "北京", 45, 78, 32, 156, 89],
    ["2023-01", "上海", 38, 65, 28, 145, 76]
  ]
}
```

## 安全注意事项

1. **CSRF保护**：所有POST请求都需要CSRF令牌
2. **登录验证**：使用`@login_required`装饰器
3. **参数验证**：验证所有输入参数
4. **文件大小限制**：防止生成过大的文件
5. **下载频率限制**：考虑实现下载频率限制

## 下一步行动

1. 你的搭档实现后端接口
2. 测试接口功能
3. 集成测试前后端交互
4. 优化用户体验（加载提示、进度条等）
5. 部署和上线测试

如果遇到具体的技术问题，可以随时讨论解决方案。
